services:
  django-app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./db-data:/app/db-data
      - ./media:/app/media
    environment:
      DEBUG: ${DEBUG}
      SECRET_KEY: ${SECRET_KEY}